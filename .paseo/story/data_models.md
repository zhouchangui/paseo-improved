# 核心数据结构与数据模型资产 (Data Models & Schema)

本文件定义本项目中所有核心的数据结构、数据库表结构、协议对象以及关键结构体模型。每次迭代若修改数据结构，必须在此增量更新。

---

## 1. 数据库表结构 (Database Schemas)

*(暂无资产定义。新增表结构后，请在此以 Markdown 表格或 DDL 形式定义，并标注 `[Updated in Iter N]`。)*

---

## 2. 关键协议与核心对象结构体 (Core Data Structures & Protocols)

*(暂无资产定义。核心 JSON、结构体、类或关键协议入参出参结构在此定义。)*

---

## 3. 资产更新日志 (Asset Update Log)

- **Initialization**: 数据模型资产库初始化建立。
