# 核心 API 与核心接口资产 (APIs & Interfaces)

本文件定义了本项目中所有向外暴露或内部调用的核心 API、服务端接口、客户端/服务端 RPC、WebSocket 路由以及关键类/服务接口。**每次迭代若修改或新增了接口，必须在此进行增量更新。**

---

## 1. 外部暴露 API (External HTTP / WebSocket APIs)

*(暂无资产定义。在迭代中编写了新的控制器或路由后，请在此增量定义，包括请求方法、路径、入参、出参和功能描述)*

---

## 2. 内部服务/类接口 (Internal Class / Service Interfaces)

*(暂无资产定义。核心的 Service 类、跨模块调用接口或基础设施接口规范)*

---

## 3. 资产更新日志 (Asset Update Log)

- **Initialization**: 接口资产库初始化建立。
